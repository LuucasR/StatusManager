node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

# Legacy SQLite file from before the Postgres move. Never commit a DB blob.
prisma/*.db
prisma/*.db-journal
